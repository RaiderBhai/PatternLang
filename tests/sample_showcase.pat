func sq(int x) {
    return x * x;
}

int n;
input n;
if (n > 5) {
     int t1 = sq(n);
     print "square: ";
     print t1;
     newline;
   } else {
     int t1 = min(7,3);
     print "min of 7 and 3: ";
     print t1;
     newline;
}

for i = 1 to 5 {
    print i;
    newline;
}